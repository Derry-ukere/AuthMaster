export enum GUEST_PERMS {
  VERIFY_EMAIL = 'guests.email-verification.allow',
  CHANGE_PASSWORD = 'guests.password-update.allow',
}

export enum USER_PERMS {
  FULL_LOGIN = 'users.global.full-login',
}
